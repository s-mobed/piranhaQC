{
  "name": "piranhaqc",
  "version": "0.1.0",
  "description": "Quality control module of PiranhaNET",
  "license": "ISC",
  "author": "smobed",
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.renderer.json",
    "start": "npm run build && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "csv-parse": "^7.0.2",
    "csv-stringify": "^6.8.3"
  }
}
